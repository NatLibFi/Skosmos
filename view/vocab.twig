{% extends "light.twig" %}
{% block title %}: {{ vocab.title(request.contentLang) }}{% endblock %}

{% block sidebar %}
<div class="sidebar-buttons">
  <ul class="nav nav-tabs{% if vocab.showChangeList and vocab.groupClassURI %} nav-four-wide{% endif %}">
    <li id="alpha"{% if active_tab == 'alphabetical' %} class="active"{% endif %}><a href="{{ request.vocabid }}/{{ request.lang }}/index{% if request.contentLang != request.lang %}?clang={{ request.contentLang }}{% endif %}">{% if vocab.showChangeList %}A-Z{% else %}{% trans "Alpha-nav" %}{% endif %}</a></li>
    <li id="hierarchy{% if not vocab.showHierarchy %}-disabled{% endif %}"{% if active_tab == 'hierarchy' %} class="active"{% endif %}><a href="#" id="hier-trigger">{% trans "Hier-nav" %}</a></li>
    {% if vocab.groupClassURI %}
    <li id="groups"><a href="{{ request.vocabid }}/{{ request.lang }}/groups{% if request.contentLang != request.lang %}?clang={{ request.contentLang }}{% endif %}">{% trans "Group-nav" %}</a></li>
    {% endif %}
    {% if vocab.showChangeList %}
    <li id="changes"><a href="{{ request.vocabid }}/{{ request.lang }}/new{% if request.contentLang != request.lang %}?clang={{ request.contentLang }}{% endif %}">{% trans "Changes-nav" %}</a></li>
    {% endif %}
  </ul>
</div>

<div class="sidebar-grey">
  <div class="group-container">
    <ul class="group-index">
      {% for uri, value in groups %}
      <li><a href="{{ uri | link_url(vocab,request.lang,'groups') }}">{{ value.label }}</a>
        {% if value.children %}{% for childuri,childlabel in value.children %}<li class="sub-group"><a href="{{ childuri | link_url(vocab,request.lang,'groups') }}">{{ childlabel }}</a></li>{% endfor %}{% endif %}
      </li>
      {% endfor %}
    </ul>
  </div>
</div>
{% endblock %}

{% block content %}
  {% include "vocab-shared.twig" %}
{% endblock %}
